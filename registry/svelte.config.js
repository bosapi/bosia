// ponytail: empty config silences svelte-check's "no svelte.config" warning.
// Bosia uses no preprocessor; lang="ts" is type-checked directly. Add vitePreprocess if that changes.
export default {};
